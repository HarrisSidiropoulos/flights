/*eslint indent: */
export const INITIAL_STATE = {}

export default function puzzle(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return {
        ...state
      }
  }
}
